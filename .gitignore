node_modules
.next
.env
.env.local
*.log
drizzle/meta
.env

# Uploaded files (logos, etc.)
/public/uploads/
# Private uploads (payment proofs) — served only via the auth-gated /api/proof route
/private-uploads/

# typescript incremental build cache
tsconfig.tsbuildinfo
